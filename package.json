{
  "private": true,
  "scripts": {
    "dev": "npm --prefix compass-react run dev",
    "build": "npm --prefix compass-react run build",
    "preview": "npm --prefix compass-react run preview",
    "vercel-build": "npm --prefix compass-react ci && npm --prefix compass-react run build"
  }
}